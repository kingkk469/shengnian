# voice-journal 依赖
# ════════════════════════════════════════════════════════════
# 安装: pip install -r requirements.txt
# 注:funasr 第一次加载会自动下载模型(几百 MB)
# ════════════════════════════════════════════════════════════

# ── GUI ──
PySide6>=6.6.0

# ── 录音 / VAD ──
sounddevice>=0.4.6
webrtcvad-wheels>=2.0.14

# ── 文件监听 ──
watchdog>=3.0.0

# ── 大模型 SDK ──
openai>=1.30.0           # DeepSeek 走 OpenAI 兼容接口
# anthropic>=0.34.0      # 用 Claude 的话取消注释

# ── 中文 ASR 转写 ──
funasr>=1.0.0
modelscope>=1.13.0
torch>=2.0.0             # funasr 依赖

# ── 贴链接抓内容 (抖音/B 站/公众号) ──
yt-dlp>=2024.0.0
trafilatura>=2.0.0
lxml_html_clean>=0.4.5   # trafilatura 2.x 必装；包含安全修复
requests>=2.33.0
rookiepy>=0.5.0          # 仅在用户显式开启浏览器 Cookie 访问时使用
aiohttp>=3.14.1          # 抖音官方 API 异步下载；包含安全修复
pyyaml>=6.0              # 抖音下载器 DouyinAPIClient 依赖
gmssl>=3.2.2             # 抖音 a_bogus 签名(国密 sm3)依赖
